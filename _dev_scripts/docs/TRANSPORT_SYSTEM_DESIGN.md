# 📋 وثيقة تصميم نظام النقل المتكامل
## نظام إدارة الشاحنات والنقل الداخلي والخارجي

---

## 📌 نظرة عامة على المشكلة الحالية

النظام الحالي يحتوي على عناصر متداخلة وغير منظمة في القائمة الجانبية:

```
القائمة الحالية (غير منظمة):
├── حركة النقليات              ❌ عام جداً
├── شاحنات النقل الخارجي      ⚠️  معزول
├── شاحنات النقل الداخلي      ⚠️  معزول
└── إدارة المستودعات         ❌ منفصل
```

**المشاكل:**
1. **عدم وضوح الهرمية** - المستخدم لا يفهم الفرق بين "حركة النقليات" و "الشاحنات"
2. **تكرار المحتوى** - ثلاث عناصر تقريباً للنقل
3. **عدم الربط مع المستودعات** - نقل قطع الغيار منفصل عن إدارة الشاحنات
4. **نقص المنطقية** - لا توجد تسلسل منطقي في الخيارات

---

## ✅ التصميم الجديد المقترح

### 1️⃣ القائمة الجانبية المنظمة الجديدة

```
═══════════════════════════════════════════════════════════════
📦 TRANSPORT & FLEET MANAGEMENT (قسم النقل وإدارة الأسطول)
═══════════════════════════════════════════════════════════════

┌─ 🚚 INTERNAL FLEET (الأسطول الداخلي - ملك الشركة)
│  ├── 📋 الشاحنات الداخلية
│  ├── 👥 سائقو الشاحنات
│  ├── 🔧 الصيانة والخدمة
│  └── 📊 تقارير الأداء
│
├─ 🚛 EXTERNAL FLEET (الأسطول الخارجي - مستأجر)
│  ├── 📋 الشاحنات الخارجية
│  ├── 📝 العقود والتسعير
│  ├── 📞 بيانات الموردين
│  └── 📊 تقارير الاستئجار
│
└─ 📦 OPERATIONS (العمليات)
   ├── 🚚 طلبات النقل الداخلي
   ├── 🚛 طلبات النقل الخارجي
   ├── 📍 تتبع الشحنات
   └── 📊 تقارير النقل

═══════════════════════════════════════════════════════════════
🏪 WAREHOUSE MANAGEMENT (إدارة المستودعات)
═══════════════════════════════════════════════════════════════
├── 📦 إدارة المستودعات
├── 📊 المخزون
├── 📥 الاستقبال
└── 📤 التصدير
```

---

## 🏗️ بنية النظام التفصيلية

### أ) الشاحنات الداخلية (Internal Trucks)
**ملكية:** الشركة
**الوظائف الأساسية:**
- إضافة وتعديل شاحنات الشركة
- تتبع بيانات المشروع (سائق، موقع، حالة التشغيل)
- إدارة الصيانة والفحوصات الدورية
- تكاليف التشغيل الفعلية (الوقود، الصيانة، المحروقات)

**الحقول المهمة:**
- رقم اللوحة (Plate Number) - فريد
- الماركة والموديل والسنة
- سعة التحميل (بالطن)
- السائق المسؤول
- الموقع الحالي
- تاريخ آخر صيانة
- حالة التشغيل (متاح/مشغول/صيانة)

**العلاقات:**
```
InternalTruck
├── hasOne Equipment (تمثيل في نظام المعدات)
├── belongsTo Driver (Employee)
├── belongsTo Location (الموقع الحالي)
├── hasMany InternalTransportOrders
└── hasMany MaintenanceLogs
```

---

### ب) الشاحنات الخارجية (External Trucks)
**الملكية:** موردين خارجيين (يتم استئجارهم)
**الوظائف الأساسية:**
- إضافة بيانات الشاحنات المستأجرة
- إدارة العقود والأسعار
- تتبع مدة الاستئجار
- إدارة معلومات السائق والمورد

**الحقول المهمة:**
- رقم اللوحة (Plate Number) - فريد
- نوع التحميل (علبة/خزان)
- سعة التحميل (حجم/وزن)
- المورد (Supplier) - الذي يملك الشاحنة
- سائق الشاحنة (الخارجي)
- رقم السائق والاتصال
- عقد الاستئجار
- السعر اليومي/الأسبوعي/الشهري
- تاريخ بدء وانتهاء العقد
- حالة الشاحنة (نشطة/غير نشطة/صيانة)

**العلاقات:**
```
ExternalTruck
├── belongsTo Supplier (المورد صاحب الشاحنة)
├── hasMany ExternalTransportOrders
├── hasMany Invoices (فواتير الاستئجار)
└── hasMany MaintenanceLogs
```

---

### ج) طلبات النقل (Transport Orders)
**الهدف:** إنشاء وتتبع طلب نقل - سواء داخلي أو خارجي

#### 📥 طلب النقل الداخلي (Internal Transport Order)

```
InternalTransportOrder {
    id
    order_number          // EXP-TRUCK-20251119-1001
    status                // pending, loading, in_transit, unloading, completed

    // الشاحنة والسائق
    internal_truck_id     // الشاحنة المستخدمة
    driver_id             // السائق

    // المسار
    loading_location_id   // موقع التحميل (مستودع)
    unloading_location_id // موقع التفريغ (موقع/مشروع)

    // التفاصيل
    load_description      // وصف البضاعة
    estimated_distance    // المسافة المقدرة
    estimated_duration    // الوقت المقدر

    // التوقيت
    requested_at          // تاريخ الطلب
    loading_scheduled_at  // وقت التحميل المجدول
    loading_started_at    // وقت بدء التحميل
    loading_completed_at  // وقت انتهاء التحميل
    departure_at          // وقت المغادرة
    estimated_arrival_at  // الوصول المقدر
    arrived_at            // وقت الوصول الفعلي
    unloading_started_at  // وقت بدء التفريغ
    unloading_completed_at// وقت انتهاء التفريغ

    // التكاليف
    fuel_cost             // تكلفة الوقود
    toll_cost             // رسوم المرور
    labor_cost            // تكلفة العمالة
    other_cost            // تكاليف أخرى
    total_cost            // الإجمالي

    // الملاحظات والمرفقات
    notes                 // ملاحظات
    issues                // مشاكل واجهتها الشحنة
    photos                // صور التحميل والتفريغ
}

// الحقول المسار
LoadingPoint {
    id
    order_id
    item_id               // ماذا يتم تحميله
    item_type             // spare_part, material, equipment
    quantity
    unit
    warehouse_location    // مكان المخزن
    loaded_quantity
    loaded_at
}
```

#### 📤 طلب النقل الخارجي (External Transport Order)

```
ExternalTransportOrder {
    id
    order_number          // EXT-TRUCK-20251119-1001
    status                // pending, contract_signed, loading, in_transit, unloading, completed

    // الشاحنة والمورد
    external_truck_id     // الشاحنة المستأجرة
    supplier_id           // المورد
    driver_name           // اسم سائق المورد
    driver_phone          // رقم هاتف السائق

    // المسار
    loading_location_id   // موقع التحميل (مستودع)
    unloading_location_id // موقع التفريغ

    // العقد والتسعير
    contract_type         // per_trip, daily, weekly, monthly
    contract_price        // السعر
    contract_start_date   // بدء العقد
    contract_end_date     // نهاية العقد

    // التفاصيل
    load_description      // وصف الحمولة
    estimated_distance    // المسافة
    estimated_duration    // المدة

    // التوقيت
    requested_at          // تاريخ الطلب
    loading_scheduled_at
    loading_started_at
    loading_completed_at
    departure_at
    estimated_arrival_at
    arrived_at
    unloading_started_at
    unloading_completed_at

    // التكاليف والدفع
    contract_price        // سعر العقد
    additional_cost       // تكاليف إضافية
    discount              // الخصم
    final_price           // السعر النهائي
    payment_status        // paid, pending, partial
    paid_amount           // المدفوع
    payment_date          // تاريخ الدفع

    // الملاحظات
    notes
    issues
    photos
    invoice_id            // فاتورة الاستئجار
}
```

---

## 🗂️ هيكل الملفات والمجلدات المقترح

```
resources/views/
├── transport/                          # 🚚 النقل العام
│   ├── index.blade.php                # لوحة النقل الرئيسية
│   └── dashboard.blade.php            # ملخص إحصائي
│
├── internal-fleet/                    # 🚚 الأسطول الداخلي
│   ├── trucks/
│   │   ├── index.blade.php           # قائمة الشاحنات
│   │   ├── create.blade.php          # إضافة شاحنة جديدة
│   │   ├── edit.blade.php            # تعديل الشاحنة
│   │   └── show.blade.php            # تفاصيل الشاحنة
│   ├── drivers/
│   │   ├── index.blade.php
│   │   └── assign.blade.php          # تعيين سائق
│   ├── maintenance/
│   │   ├── index.blade.php           # سجل الصيانة
│   │   └── create.blade.php          # إضافة صيانة
│   └── reports/
│       ├── performance.blade.php
│       ├── maintenance.blade.php
│       └── costs.blade.php
│
├── external-fleet/                    # 🚛 الأسطول الخارجي
│   ├── trucks/
│   │   ├── index.blade.php
│   │   ├── create.blade.php
│   │   ├── edit.blade.php
│   │   └── show.blade.php
│   ├── contracts/
│   │   ├── index.blade.php           # العقود
│   │   ├── create.blade.php
│   │   ├── edit.blade.php
│   │   └── show.blade.php
│   ├── suppliers/
│   │   └── index.blade.php           # بيانات الموردين
│   └── reports/
│       ├── rental.blade.php
│       ├── costs.blade.php
│       └── payments.blade.php
│
├── transport-operations/              # 📦 عمليات النقل
│   ├── internal-orders/
│   │   ├── index.blade.php           # طلبات النقل الداخلي
│   │   ├── create.blade.php
│   │   ├── edit.blade.php
│   │   ├── show.blade.php
│   │   └── tracking.blade.php        # متابعة الحالة
│   ├── external-orders/
│   │   ├── index.blade.php           # طلبات النقل الخارجي
│   │   ├── create.blade.php
│   │   ├── edit.blade.php
│   │   ├── show.blade.php
│   │   └── tracking.blade.php
│   ├── tracking/
│   │   └── index.blade.php           # تتبع الشحنات
│   └── reports/
│       ├── summary.blade.php
│       ├── details.blade.php
│       └── costs.blade.php
│
└── warehouses/                        # 🏪 المستودعات
    └── [المجلد الموجود]
```

---

## 🎯 التدفق العملياتي

### 1️⃣ عملية النقل الداخلي

```
مسؤول المستودع
    ↓
① يختار "طلب نقل داخلي جديد"
    ↓
② يختار الشاحنة الداخلية المتاحة
    ↓
③ يحدد موقع التحميل (المستودع) وموقع التفريغ
    ↓
④ يضيف قطع الغيار/المواد المراد نقلها
    ↓
⑤ ينتظر تأكيد السائق
    ↓
⑥ التحميل يبدأ ← يسجل وقت البدء
    ↓
⑦ التحميل ينتهي ← يسجل وقت الانتهاء
    ↓
⑧ السائق يغادر الموقع
    ↓
⑨ تتبع الشحنة في الطريق (GPS اختياري)
    ↓
⑩ الوصول للموقع الثاني
    ↓
⑪ التفريغ يبدأ
    ↓
⑫ التفريغ ينتهي ← تحديث المخزون تلقائياً
    ↓
⑬ إغلاق الطلب وحفظ الملخص
```

### 2️⃣ عملية النقل الخارجي

```
مسؤول المشروع
    ↓
① يختار "طلب نقل خارجي جديد"
    ↓
② يختار المورد المطلوب
    ↓
③ يختار الشاحنة من المورد
    ↓
④ يحدد نوع العقد (رحلة واحدة/يومي/أسبوعي)
    ↓
⑤ يحدد التواريخ والمواقع
    ↓
⑥ يضيف الحمولة والمواد
    ↓
⑦ ينتظر قبول المورد
    ↓
⑧ توقيع العقد
    ↓
⑨ التحميل
    ↓
⑩ النقل
    ↓
⑪ التفريغ
    ↓
⑫ إصدار فاتورة
    ↓
⑬ الدفع
    ↓
⑭ إغلاق الطلب
```

---

## 📊 الفرق الرئيسي بين النوعين

| المعيار | نقل داخلي 🚚 | نقل خارجي 🚛 |
|--------|-----------|-----------|
| **الملكية** | تابعة للشركة | موردين خارجيين |
| **التحكم** | مراقبة كاملة | اتفاقيات عقود |
| **السائق** | موظفي الشركة | سائق المورد |
| **التكاليف** | بالفعل في الميزانية | عقود/بالرحلة |
| **المسؤولية** | الشركة كاملة | الشركة + المورد |
| **التحديث التلقائي** | نعم | يدوي بعد الوصول |
| **الفاتورة** | لا (داخلي) | نعم (من المورد) |
| **الصيانة** | من قبل الشركة | من قبل المورد |

---

## 🔧 النماذج المطلوبة إنشاؤها

### 1. TransportOrder (الأساس المشترك)
```
- id
- order_number
- status
- transport_type (internal/external)
- loading_location_id
- unloading_location_id
- load_description
- requested_at
- notes
- created_by
- created_at
```

### 2. InternalTransportOrder (النقل الداخلي)
```
- transport_order_id (FK)
- internal_truck_id
- driver_id
- fuel_cost
- toll_cost
- labor_cost
- other_cost
- total_cost
```

### 3. ExternalTransportOrder (النقل الخارجي)
```
- transport_order_id (FK)
- external_truck_id
- supplier_id
- contract_type
- contract_price
- additional_cost
- discount
- final_price
- payment_status
- invoice_id
```

### 4. TransportOrderItem (تفاصيل الحمولة)
```
- id
- transport_order_id
- item_id
- item_type (spare_part/material/equipment)
- quantity
- unit
- loaded_quantity
- unloaded_quantity
```

---

## 🎨 الألوان والأيقونات المقترحة

```
الأسطول الداخلي:     🚚 أزرق (Blue)      - #3B82F6
الأسطول الخارجي:     🚛 أحمر (Red)       - #EF4444
طلبات النقل:         📦 أخضر (Green)    - #10B981
التتبع والمتابعة:    📍 برتقالي (Orange)- #F97316
التقارير:            📊 بنفسجي (Purple) - #8B5CF6
```

---

## ✨ المزايا التي ستحصل عليها

✅ **تنظيم واضح** - فصل كامل بين الداخلي والخارجي
✅ **سهولة الاستخدام** - واجهة بديهية وسهلة
✅ **تتبع دقيق** - معرفة موقع الشحنة في كل لحظة
✅ **إدارة التكاليف** - حساب تكاليف كل نقلة
✅ **التقارير** - تقارير شاملة عن الأداء والتكاليف
✅ **التكامل** - ربط تلقائي مع المخزون والفواتير
✅ **المرونة** - دعم أنواع مختلفة من العقود

---

## 📝 خطة التنفيذ

**المرحلة 1:** إنشاء النماذج والجداول (Migrations)
**المرحلة 2:** إنشاء Controllers
**المرحلة 3:** بناء Views
**المرحلة 4:** إضافة المميزات المتقدمة
**المرحلة 5:** التقارير والتحليلات
**المرحلة 6:** التكامل مع الأنظمة الأخرى

---

**تاريخ الوثيقة:** 19 نوفمبر 2025
**الإصدار:** 1.0